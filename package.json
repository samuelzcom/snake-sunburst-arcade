{
  "name": "snake-sunburst-arcade",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "node -e \"console.log('lint: no-op')\"",
    "typecheck": "node -e \"console.log('typecheck: no-op')\"",
    "test": "node --test tests/*.test.mjs",
    "validate": "npm run -s lint && npm run -s typecheck && npm run -s test && npm run -s build"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.26.0",
    "vite": "^6.1.0"
  },
  "packageManager": "npm@10.8.2",
  "engines": {
    "node": ">=20"
  }
}
