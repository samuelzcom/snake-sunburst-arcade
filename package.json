{
  "name": "snake-sunburst-arcade",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "node scripts/check-syntax.mjs",
    "typecheck": "tsc --project tsconfig.json",
    "test": "node --test tests/*.test.mjs",
    "validate": "npm run -s lint && npm run -s typecheck && npm run -s test && npm run -s build"
  },
  "dependencies": {
    "@cloudflare/vite-plugin": "^1.27.0",
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  },
  "packageManager": "npm@10.8.2",
  "engines": {
    "node": ">=20"
  }
}
